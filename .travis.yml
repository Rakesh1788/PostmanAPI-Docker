language: node_js
sudo: required

node_js:
- "6.14.4"

services:
  - docker

before_install:
  - docker build -t raks/postmanapi-docker -f Dockerfile .

script:
   - docker run -e CI=true -t raks/postmanapi-docker:latest run /etc/newman/collection1.json
  
