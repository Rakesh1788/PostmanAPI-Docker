language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t raks/docker-react -f Dockerfile .
  
script:
   - docker run -t postman/newman_ubuntu1404 run /tmp/collections/collection2.json 
  
