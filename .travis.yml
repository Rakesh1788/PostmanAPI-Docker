language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t postman/newman_ubuntu1404 -f Dockerfile .
  
script:
   - docker run -t postman/newman_ubuntu1404 run /tmp/collections/collection2.json 
  
